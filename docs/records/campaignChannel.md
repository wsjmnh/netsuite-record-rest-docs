# campaignChannel

Browser tag `campaignChannel` (Record REST v1, 2026.1).

Base path: `https://{accountId}.suitetalk.api.netsuite.com/services/rest/record/v1` — collection `/campaignChannel`, instance `/campaignChannel/{id}`.

Shared query/header conventions: [overview](../overview.md).

## Methods

| Method | Path | Operation ID | Summary | Request body | Responses |
| --- | --- | --- | --- | --- | --- |
| DELETE | `/campaignChannel` | `operation--campaignChannel-delete` | Delete a list of records. |  | 202 Accepted; default → `nsError` |
| GET | `/campaignChannel` | `operation--campaignChannel-get` | Get list of records. |  | 200 OK → `campaignChannelCollection`; 202 Accepted; default → `nsError` |
| PATCH | `/campaignChannel` | `operation--campaignChannel-patch` | Update records. | `campaignChannelCollection` | 202 Accepted; default → `nsError` |
| POST | `/campaignChannel` | `operation--campaignChannel-post` | Insert record. | `campaignChannel` | 200 OK → `campaignChannel`; 202 Accepted; 204 No Content; default → `nsError` |
| PUT | `/campaignChannel` | `operation--campaignChannel-put` | Insert or update records. | `campaignChannelCollection` | 202 Accepted; default → `nsError` |
| DELETE | `/campaignChannel/{id}` | `operation--campaignChannel--id--delete` | Remove record. |  | 202 Accepted; 204 No Content; default → `nsError` |
| GET | `/campaignChannel/{id}` | `operation--campaignChannel--id--get` | Get record. |  | 200 OK → `campaignChannel`; 202 Accepted; default → `nsError` |
| PATCH | `/campaignChannel/{id}` | `operation--campaignChannel--id--patch` | Update record. | `campaignChannel` | 200 OK → `campaignChannel`; 202 Accepted; 204 No Content; default → `nsError` |
| PUT | `/campaignChannel/{id}` | `operation--campaignChannel--id--put` | Insert or update record. | `campaignChannel` | 202 Accepted; 204 No Content; default → `nsError` |

## Parameters

Parameters that appear on this tag’s operations. Shared names are defined in the overview; this table only records presence, location, required, type, format, enum, and default.

| Operation ID | Method / path | Parameters |
| --- | --- | --- |
| `operation--campaignChannel-delete` | DELETE `/campaignChannel` | `ids`; in query; required; string · `Prefer`; in header; required; string; enum `respond-async` · `X-NetSuite-Idempotency-Key`; in header; string |
| `operation--campaignChannel-get` | GET `/campaignChannel` | `q`; in query; string · `limit`; in query; integer (int32); default `1000` · `offset`; in query; integer (int32); default `0` · `expandRecords`; in query; boolean · `ids`; in query; string · `Prefer`; in header; string; enum `respond-async` · `X-NetSuite-Idempotency-Key`; in header; string |
| `operation--campaignChannel-patch` | PATCH `/campaignChannel` | `X-NetSuite-PropertyValueValidation`; in header; string; default `Error`; enum `Error`, `Warning`, `Ignore` · `X-NetSuite-PropertyNameValidation`; in header; string; default `Warning`; enum `Error`, `Warning`, `Ignore` · `replace`; in query; string · `replaceSelectedFields`; in query; boolean; default `false` · `Prefer`; in header; required; string; enum `respond-async` · `X-NetSuite-Idempotency-Key`; in header; string |
| `operation--campaignChannel-post` | POST `/campaignChannel` | `Prefer`; in header; string; enum `respond-async` · `X-NetSuite-Idempotency-Key`; in header; string · `fields`; in query; string · `q`; in query; string · `limit`; in query; integer (int32); default `1000` · `offset`; in query; integer (int32); default `0` · `expandSubResources`; in query; boolean; default `false` · `expand`; in query; string · `Accept`; in header; string; enum `application/vnd.oracle.resource+json; type=create-form`, `application/vnd.oracle.resource+json; type=select-options` · `replace`; in query; string · `X-NetSuite-PropertyNameValidation`; in header; string; default `Warning`; enum `Error`, `Warning`, `Ignore` · `X-NetSuite-PropertyValueValidation`; in header; string; default `Error`; enum `Error`, `Warning`, `Ignore` |
| `operation--campaignChannel-put` | PUT `/campaignChannel` | `X-NetSuite-PropertyValueValidation`; in header; string; default `Error`; enum `Error`, `Warning`, `Ignore` · `X-NetSuite-PropertyNameValidation`; in header; string; default `Warning`; enum `Error`, `Warning`, `Ignore` · `replace`; in query; string · `replaceSelectedFields`; in query; boolean; default `false` · `Prefer`; in header; required; string; enum `respond-async` · `X-NetSuite-Idempotency-Key`; in header; string |
| `operation--campaignChannel--id--delete` | DELETE `/campaignChannel/{id}` | `id`; in path; required; integer (int32); internal identifier · `Prefer`; in header; string; enum `respond-async` · `X-NetSuite-Idempotency-Key`; in header; string |
| `operation--campaignChannel--id--get` | GET `/campaignChannel/{id}` | `id`; in path; required; integer (int32); internal identifier · `expandSubResources`; in query; boolean; default `false` · `simpleEnumFormat`; in query; boolean; default `false` · `fields`; in query; string · `Prefer`; in header; string; enum `respond-async` · `X-NetSuite-Idempotency-Key`; in header; string |
| `operation--campaignChannel--id--patch` | PATCH `/campaignChannel/{id}` | `Prefer`; in header; string; enum `respond-async` · `X-NetSuite-Idempotency-Key`; in header; string · `fields`; in query; string · `q`; in query; string · `limit`; in query; integer (int32); default `1000` · `offset`; in query; integer (int32); default `0` · `expandSubResources`; in query; boolean; default `false` · `expand`; in query; string · `Accept`; in header; string; enum `application/vnd.oracle.resource+json; type=edit-form`, `application/vnd.oracle.resource+json; type=select-options` · `id`; in path; required; integer (int32); internal identifier · `X-NetSuite-PropertyNameValidation`; in header; string; default `Warning`; enum `Error`, `Warning`, `Ignore` · `X-NetSuite-PropertyValueValidation`; in header; string; default `Error`; enum `Error`, `Warning`, `Ignore` · `replace`; in query; string · `replaceSelectedFields`; in query; boolean; default `false` |
| `operation--campaignChannel--id--put` | PUT `/campaignChannel/{id}` | `id`; in path; required; string; external identifier · `Prefer`; in header; string; enum `respond-async` · `X-NetSuite-Idempotency-Key`; in header; string · `X-NetSuite-PropertyNameValidation`; in header; string; default `Warning`; enum `Error`, `Warning`, `Ignore` · `X-NetSuite-PropertyValueValidation`; in header; string; default `Error`; enum `Error`, `Warning`, `Ignore` · `replace`; in query; string · `replaceSelectedFields`; in query; boolean; default `false` |

## Schema refs

Definitions owned by this record (property tables): [campaignChannel schemas](../schemas/campaignChannel.md).

| Definition | Role |
| --- | --- |
| [`campaignChannel`](../schemas/campaignChannel.md#campaignchannel) | record body |
| [`campaignChannelCollection`](../schemas/campaignChannel.md#campaignchannelcollection) | collection page |
| [`campaignChannelSelectOptions`](../schemas/campaignChannel.md#campaignchannelselectoptions) | select-options |

Other schemas referenced by this tag’s operations:

- [`nsError`](../schemas/ns.md#nserror)

## Sublists

No `*Collection` properties on the record schema other than the record’s own collection type.
